# Skills Testing Directory

This subfolder is dedicated to testing custom agent skills locally. 

You can place schema files, JSON files, code files, and git commit messages here to validate the execution of:
* `always-verify-gcp`
* `git-commit-formatter`
* `license-header-adder`
* `database-schema-validator`
* `json-to-pydantic`
